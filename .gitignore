.env
.venv
__pycache__/
data/**/*.pdf
data/processed/*
!data/processed/.gitkeep
eval/results/*
!eval/results/.gitkeep
scratchpad/
.DS_Store

# stray eval / tooling logs (e.g. likeforlike_run.log, router_eval.log) — never committed
*.log

# same-process A/B repro probe (scratch — not committed)
scripts/ab_repro_probe.py
scripts/ab_repro_probe_result.json
scripts/ab_repro_probe_control_result.json

# decomposition falsification probe (Step 8) — script + result are scratch; PREDICTION is committed
scripts/decomp_probe*
!scripts/decomp_probe_PREDICTION.md

# re-chunk design pass (Step 9) — pre-chunk baseline snapshot is scratch (the frozen "before")
scripts/prechunk_top10_baseline.json

# like-for-like eval scratch (gitignored; result in the committed pre-reg doc)
scripts/likeforlike_eval.py
scripts/likeforlike_result.json
